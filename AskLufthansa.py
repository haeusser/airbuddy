__author__ = 'Philip'

import datetime
import time


def get_flight_status(airline_symbol, flight_number, flight_date):
    # TODO: check if flight date is in the future
    # today = datetime.datetime.today()

    return "FAKE FLIGHTSTATUS: " + str(airline_symbol) + str(flight_number) + str(
        flight_date)